import ManageTeams from "@/components/basejump/manage-teams";

export default async function PersonalAccountTeamsPage(): Promise<JSX.Element> {
  return (
    <div>
      <ManageTeams />
    </div>
  );
}
